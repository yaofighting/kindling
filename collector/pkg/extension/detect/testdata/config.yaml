extension:
  dial:
    enable: true
    debug: true
    endpoint: "http://apm-data-receiver:8080"
    insecure: true
    timeout: 10s
    read_buffer_size: 4096
    write_buffer_size: 4096
    compression: gzip
    batch_timeout: 10s
    send_batch_size: 400
    send_batch_max_size: 420